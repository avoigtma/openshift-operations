# Example OpenShift Operations Tasks

* backup of individual namespaces (manifests only, no PV data)
* pruning of images, builds, deployments, etc.
* update-graph: create update graph representation for OpenShift

